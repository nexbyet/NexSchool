

<IfModule mod_rewrite.c>
    RewriteEngine On
    
    # Allow direct access to existing files (phpinfo, assets)
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteCond %{REQUEST_FILENAME} !-d
    
    # Route everything else to public/
    RewriteRule ^(.*)$ public/$1 [L]
</IfModule>
